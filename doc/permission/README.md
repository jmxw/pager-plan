# Permissions

One doc per `UserPermission` value, grouped by area. The enum constant is copied verbatim; gated
endpoints are referenced by link (the reverse index of each API doc's permission). Role hierarchy:
OWNER > ADMIN > DEVELOPER > CUSTOMER. See [rules/permissions.md](../../rules/permissions.md).
